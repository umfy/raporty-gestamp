extends layout
block content
   div(class="col mx-md-5 my-5")
      h1 Usuń raport: 
      h1 #{raport.virtual_date}   
      h2 Zmiana #{raport.shift}
      


      p.mt-4 Czy na pewno chcesz usunąć ten raport?

         form(method='POST' action='')
            div.form-group
               input#raportId.form-control(type='hidden',name='raportId', required='true', value=raport._id )

            button.btn.btn-danger(type='submit') Usuń