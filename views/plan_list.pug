extends layout

block content
   h1= title

   ul 
      each plan in plan_list
         li
            a(href=plan.url) #{plan.date_created}
            p #{plan.desc}
      else
         li There are no lines.